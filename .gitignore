# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# Env — keep secrets out of git. `.env.example` is the only env file that may be tracked.
.env
.env.*
!.env.example
secrets.json
credentials.json
serviceAccount*.json
*.pem
*.p12

node_modules
.venv-bg
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel CLI link (project/org IDs) — never commit
.vercel

# OS junk that sometimes sneaks into public/
**/.DS_Store
