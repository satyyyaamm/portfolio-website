/**
 * Tech words revealed one at a time across the journey chapter, then gathered
 * into a grid that hands off to the project photo grid.
 *
 * Order is roughly the order they entered the work, so the sequence reads as a
 * path rather than a list.
 */
export const TECH_WORDS = ['Flutter', 'Dart', 'iOS', 'Android', 'Firebase'];
